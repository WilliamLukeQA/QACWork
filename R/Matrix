physics <- c(95,29,68)
chemistry <- c(89,23,54)
maths <- c(43,62,27)

results <- cbind(physics,chemistry,maths)

colnames(results)<-c("physics","chemistry","maths")
rownames(results)<-c("luke","b","c")

print(results["luke","physics"])

print(rowSums(results))
